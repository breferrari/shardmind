# Dependencies
node_modules/

# Build output (tsup)
dist/
*.tsbuildinfo

# Pastel generated
source/commands/*.js
source/commands/*.d.ts
source/commands/*.map

# TypeScript cache
*.tsbuildinfo
tsconfig.tsbuildinfo

# Test
coverage/
.vitest/

# Environment
.env
.env.local
.env.*.local

# npm
*.tgz
.npm/
npm-debug.log*

# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~
Desktop.ini
ehthumbs.db

# IDE
.idea/
.vscode/
*.code-workspace
*.sublime-project
*.sublime-workspace

# Lock file alternatives (we use package-lock.json)
pnpm-lock.yaml
bun.lock
bun.lockb
yarn.lock

# npm pack output
*.tgz

# Agent auto-generated memory (never commit)
# NOTE: .claude/ as a whole is NOT ignored — it contains managed files
# (commands, agents, scripts, skills, settings.json) that ShardMind installs.
# Only ignore the auto-memory subdirectories that agents generate at runtime.
.claude/memory/
.claude/scheduled_tasks.lock
.claude/projects/
.agent/
memory/
IDENTITY.md
USER.md

# Local scratch
tmp/
.tmp/
.local/
.artifacts/
/.smoke/
