import {Text} from 'ink';

export default function Index() {
	return <Text>ShardMind v0.1.0 — run shardmind --help for usage</Text>;
}

export const description = 'Show shard status for the current vault';
