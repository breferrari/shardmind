name: "Managed file, values changed"
description: >
  The template body is unchanged, but the user's values changed (e.g. renamed
  themselves from Alice to Bob). The file should be silently re-rendered.

ownership_before: managed
user_edited: false
template_changed: false
values_changed: true
conflict_expected: false
expected_action: overwrite

values:
  old: { user_name: "Alice" }
  new: { user_name: "Bob" }
