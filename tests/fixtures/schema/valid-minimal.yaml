schema_version: 1

values:
  user_name:
    type: string
    required: true
    message: "Your name"
    group: setup

groups:
  - id: setup
    label: "Setup"

modules: {}

frontmatter:
  global: [date, description]

migrations: []
