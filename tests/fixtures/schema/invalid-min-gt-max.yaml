schema_version: 1

values:
  max_depth:
    type: number
    message: "Maximum depth"
    default: 0
    min: 10
    max: 5
    group: setup

groups:
  - id: setup
    label: "Setup"

modules: {}

frontmatter: {}

migrations: []
